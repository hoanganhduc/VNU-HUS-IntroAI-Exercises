# Week 2 — Propositional Logic: Exercise 2.5 scaffold

**Bibliographic reference:** Wolfgang Ertel, *Introduction to Artificial
Intelligence*, 3rd ed. (2025), Exercise 2.5, p. 39.

This public package does not include or restate the exercise. Obtain an
authorized copy before implementing it. The reusable scaffold does not prescribe
a programming language or input/output protocol.

## Classroom50 workflow

Read the shared [Classroom50 graphical workflow](docs/CLASSROOM50-WEB-UI.md).

Accept through the web page or, if instructed, with:

```bash
gh student accept VNU-HUS <classroom> ch02-propositional-logic
```

## Required submission

Add at least one separate, nonempty implementation file below `solution/`.
The supplied `solution/README.md` is explanatory and does not count.

```bash
python3 check_submission.py
git add solution
git commit -m "Submit Exercise 2.5 code"
git push
gh student submit
```

The checker verifies only safe paths and the presence of nonempty student code.
It neither compiles nor executes that code.

```text
100/100  complete submission
0/100    incomplete submission
```
